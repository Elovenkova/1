#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i, n, A_x, A_y, B_x, B_y, x, y;
    scanf ("%d", &n);
    int array[n];
    int E[];
    for(i=0; i<n; i++)
    {
        scanf("%d", &array[i]);
    }
    A_x = array[0];
    A_y = array[1];
    B_x = array[2];
    B_y = array[3];
    x = abs(A_x-B_x);
    y = abs(A_y-B_y);
    for(i=4; i<n; i+2)
    {
        if (abs(array[i]-A_x)<=x)
        {
            x = abs(array[i]-A_x);
            
            
        }
    }

    
}
